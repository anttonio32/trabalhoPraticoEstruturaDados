package main;
import java.util.LinkedList;

public class Fila<T extends Comparable>{
    private LinkedList<T> fila;

    public Fila() {this.fila = new LinkedList<T>();}
    
    public void addOrdemPrioridade(T outro){
        if (outro.compareTo(outro)) {
            
        }
    }
}
